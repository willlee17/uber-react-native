import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10, 
    height: '100%',
  }, 
  textInput: {
    backgroundColor: '#eee',
    // height: 40, 
    padding: 10,
    marginVertical: 5,
  }
})

export default styles;